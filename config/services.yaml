parameters:
    api_key: '%env(API_KEY)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    App\Security\ApiKeyAuthenticator:
        arguments:
            $apiKey: '%api_key%'
